export const MODE = import.meta.env.MODE;

export const PAGE_LIMITS = [10, 25, 50, 100];

export const API_ENTRYPOINT = "https://mock-api.gg";

export const enum SORT_DIRECTION {
  ASC = "asc",
  DESC = "desc",
}
